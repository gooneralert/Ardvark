#pragma once

namespace ng_tabs
{
	// the LiquidUI example's "Customize" page, ported: live glass-material
	// knobs + appearance + accent palette (see tabs/customize.cpp)
	void draw_customize_tab();
}