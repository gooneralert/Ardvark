#pragma once

namespace ng_tabs
{
	// reference pages under Visuals: Players (enemy esp) and Extras
	void draw_esp_tab();        // Players page
	void draw_extras_page();    // Extras page (loot / corpses / npc bits)
	void draw_npc_page();       // NPC page
}
